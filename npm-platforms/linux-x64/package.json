{
  "name": "@voxrouter/cli-linux-x64",
  "version": "0.1.0",
  "description": "VoxRouter CLI native binary for linux-x64 (glibc). Installed via @voxrouter/cli; do not install directly.",
  "os": ["linux"],
  "cpu": ["x64"],
  "libc": ["glibc"],
  "files": ["voxrouter"],
  "publishConfig": {
    "access": "public",
    "tag": "latest"
  }
}
